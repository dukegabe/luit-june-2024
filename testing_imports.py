import random  # Import the random module to generate random numbers

number = random.randint(0, 10)  # Generate a random integer between 0 and 10 (inclusive)

print(number)  # Print the randomly generated number
